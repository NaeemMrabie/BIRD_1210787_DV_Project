# Design folder

Place the BIRD RTL from the provided EDA Playground `design.sv` here as:

```text
design/bird.sv
```

The verification environment is written from the specification and must not be changed to match a buggy RTL implementation. If the scoreboard fails, inspect the failure as a possible DUT/spec mismatch.
